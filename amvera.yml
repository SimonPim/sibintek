meta:
  environment: docker
  toolchain:
    name: docker
    version: latest

build:
  dockerfile: Dockerfile

run:
  containerPort: 8000
  persistentMount: /app

volumes:
  - /app/db.sqlite3
  - /app/media
  - /app/staticfiles
